# "Search by text" service  

### Запуск:

#### Сборка и запуск:

- \$docker-compose up -d --build

#### Добавление базы данных в PostgreSQL:

- \$docker-compose exec web python manage.py create_db

После данных операций сервис полностью работоспособен.